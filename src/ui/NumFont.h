#pragma once

#include <lvgl.h>

// A Montserrat face carrying only digits, '-' and '.', generated by
// tools/gennumfont.py from the same TTF LVGL builds its own fonts from.
//
// It exists because LVGL's built-in Montserrat stops at 48pt, and the distance
// to the next turn is the one number on this panel that has to be read without
// looking straight at it. Its digits are 64px tall against the 34px the 48pt
// face manages.
//
// Restricted to the characters FormatTbtDistance can actually produce --
// "157", "1.2", and the "--" shown when a turn arrives without a distance.
// A full face at this size would spend tens of kilobytes of flash on glyphs
// nothing here can render.
//
// Defined in a .c file, so C linkage.
#ifdef __cplusplus
extern "C" {
#endif

extern const lv_font_t pegasus_font_num_88;

// The second page's four figures: live speed, average speed, live heart rate,
// average heart rate. Emboldened, because Montserrat Medium is the only weight
// LVGL vendors and a thin face is the first thing a rider loses in bright sun,
// so the generator draws these with an outline stroke rather than adding a
// second TTF to the repository.
//
// 46 with a 2px stroke, and both numbers are measured rather than chosen. The
// live figure and its average share a row, so each gets half the panel, and
// the widest string either can hold -- "99.9" -- comes to 110px against the
// 112 a 120px cell offers at this page's 4px inset. 47 needs 114 and does not
// fit; 48 needs 117.
extern const lv_font_t pegasus_font_num_46b;

#ifdef __cplusplus
}
#endif
